# Googleドキュメント納品仕様｜SIer転職記事（基準日：2026-08-30）

対象原稿：`docs/articles/SIer転職_本文_20260830.html`
納品形式：**Googleドキュメント**（WP入稿はしない）。HTMLは変換元の原稿であり、そのまま貼らない。

---

## 1. 冒頭【納品用メタ情報】ブロック（2026-08-30 改訂：本文HTMLから完全移設）

**本文HTML（`docs/articles/SIer転職_本文_20260830.html`）は h1〜関連記事リストのみの純粋な本文ファイルとし、メタ情報・社内確認事項・計測仕様は一切含めない。**（旧版は本文HTML冒頭と末尾に社内向けブロックが同居していたため、Googleドキュメント化時の消し忘れリスクがあった。以下に全量を移設済み。）

Googleドキュメント側では、このブロックをドキュメント最上部に置き、**本文とは水平罫線で分離**する。

- 見出しスタイルは当てない（「標準テキスト」のまま。目次に混入させないため）。
- 背景色を薄いグレー（#F1F3F4）にし、編集者向け情報であることを視覚的に示す。
- 「挿入 → 目次」を使う場合は、このブロックより下に置く。

### タイトル案（全角実測値。日本語SERPの表示上限は概ね28〜32全角）

| 案 | 文言 | 実測字数 |
|---|---|---|
| ①（推奨） | SIer転職の年収と転職先5選｜上流工程だけの経歴の書き方 | 29字 |
| ② | SIer転職は難しい？転職先5タイプの年収と難易度 | 25字 |
| ③ | SIer転職で年収は上がる？上流工程だけの経歴の書き方 | 27字 |

- 案①は h1 と同一文言（本文冒頭に反映済み）。主KW「SIer転職」を先頭に置き、差別化要素「経歴の書き方」が表示可視範囲に収まる長さ。
- 旧案の「年収レンジ」は、本文の比較表で5タイプ中2タイプしか公開統計の数値を出せていないため**全案から削除**（SERPの約束と本文の一致を優先）。

### メタディスクリプション案（99字）

> SIer転職の年収と難易度を転職先5タイプ別に整理。上流工程しかない経歴を市場の言葉に翻訳する4ステップと変換表、職務経歴書の書き換え例、年代別の勝ち筋、担当者で選ぶ相談先の見極め方まで解説します。

- モバイル表示（概ね70字）を考慮し、主KWと結論（上流工程しかない経歴を翻訳する）を前半70字に寄せている。
- 「年収レンジを公開調査データで比較」という旧案の訴求は、実態（数値が出せているのは2タイプ）と乖離するため削除済み。

### CTA実装指示（intro と cta1〜cta4 で書き分ける）

- **cta1〜cta4 はボタン要素**（`<div class="cta-box">` ＋ `<a class="btn">`）として実装する。本文リンクに潰さないこと。
- **intro（導入文末）はボタンにしない。** 本文中のテキストリンク2本のまま実装する。導入直後の読者は選択肢調査の段階で面談への心理的距離が最も遠いため、記事内アンカー（比較表）と `/agent/`（見るだけで完結する最低コストの接点）の2択に留める設計。`data-cta` 属性は同様に付与する。
- 計測属性の付け方は「7. 計測」を参照。

### 内部リンク指示

- 既存記事「転職エージェントの担当者は変更できる？」（`docs/articles/転職エージェント担当者変更_本文_20260830.html`）の**公開URLが未確定**のため、本文HTMLからはプレースホルダ（旧 `{{URL:担当者変更記事}}`）を**全撤去済み**。該当箇所は通常テキストに戻してある（本文中2箇所＋関連記事リスト1件）。
- 公開URL確定後、次の3箇所にリンクを差し込む。
  1. h3「相談先は『求人数』ではなく『担当者』で選ぶ」末尾の「既存記事『転職エージェントの担当者は変更できる？』で解説しています」
  2. FAQ「転職エージェントは使ったほうがよいですか？担当者は選べますか？」末尾の同文
  3. 記事末「関連記事」の「転職エージェントの担当者は変更できる？（公開後にリンクを設置）」
- **本文HTMLに `{{}}` 記法を残したまま納品しないこと。**

## 2. 見出しスタイルのマッピング

| 原稿HTML | Googleドキュメントのスタイル | 補足 |
|---|---|---|
| `<h1>` | 見出し1 | 記事タイトル。1本のみ |
| `<h2>` | 見出し2 | 8本 |
| `<h3>` | 見出し3 | 25本 |
| `<p>` | 標準テキスト | |
| `<strong>` | 太字 | 色は付けない |
| `<ul><li>` | 箇条書き（黒丸） | 入れ子にしない |

- **h4は使用しない。** 段落で整理する。
- 「挿入 → 目次」で目次を作る場合は、冒頭メタ情報ブロックより下に置く。

## 3. CTAブロック（`<div class="cta-box">` と `<a class="btn">`）

CSSクラスはGoogleドキュメントで効かないため、**指示書ブロック形式**に整形して1×1セルの表として表現する。

- 表の枠線：1pt、罫線色 #999999
- セル背景：#F5F5F5
- ボタン文言の行：**太字＋下線付きリンク**（本文リンクと区別できる見た目にする）
- 各ブロックの先頭に `■CTA①（設置位置：◯◯の直後）` を置く

記載する項目（4箇所すべて共通）：

```
■CTA①（設置位置：h2「上流工程しか経験がない経歴を…4ステップ」の直後）
リード文：…
ボタン文言：担当アドバイザーを選んで相談する
遷移先URL：https://stock-sun.com/nensyuagent/
計測属性：data-cta="cta1" data-article="sier-tenshoku"
補足文：（あれば）
```

### CTA一覧（ボタンCTAは4箇所＝ハウス標準の上限内。intro はテキストリンクのためCTA数に数えない）

| ID | 種別 | 設置位置 | 文言 | 遷移先 |
|---|---|---|---|---|
| intro_anchor | テキスト | 導入文末 | 転職先5タイプの比較表 | `#tenshokusaki`（記事内アンカー） |
| intro_agent | テキスト | 導入文末 | アドバイザーのプロフィール | /nensyuagent/agent/ |
| cta1 | ボタン | h2「転職先5つと年収の目安・難易度」比較表の注記直後 | 自分の経歴で狙えるレンジを求人で確認する | /nensyuagent/ |
| cta2 | ボタン | h2「職務経歴書と志望動機での具体的な書き方」末尾（本命） | 経歴を見てくれる担当者を自分で選ぶ | /nensyuagent/ |
| cta3 | ボタン | h3「相談先は『求人数』ではなく『担当者』で選ぶ」直後 | 在籍アドバイザーのプロフィールを見る | /nensyuagent/agent/ |
| cta3_voice | テキスト | cta3 ボックス内の補足行 | 転職者の声 | /nensyuagent/voice/ |
| cta4 | ボタン | 記事末まとめの直後 | 職務経歴の棚卸しから相談する | /nensyuagent/ |
| faq_link | テキスト | FAQ最終問 | 年収エージェント | /nensyuagent/ |
| faq_reputation | テキスト | FAQ最終問 | 年収エージェントの評判・口コミ | /column/annual-income-agent-reputation/ |
| related_reputation | テキスト | 関連記事 | 年収エージェントの評判 | /column/annual-income-agent-reputation/ |
| related_about | テキスト | 関連記事 | 年収エージェントとは | /nensyuagent/about/ |
| related_voice | テキスト | 関連記事 | 転職者の声・年収アップ事例 | /nensyuagent/voice/ |

**CTA配置の設計意図（2026-08-30 改訂）**

- 旧版は導入CTAから次のCTAまで約3,000字のCTA空白帯があり、読者の購買意欲が最も上がる「比較表を見た直後」にCTAがなかった。比較表直下に cta1 を新設して解消。
- 本命CTAは、独力の限界を最も強く実感する地点（書き換え表と志望先別の出し分けを読み終えた直後）へ移設し、cta2 とした。旧 cta1（4ステップ末尾）は cta1 と近接するため統合・削除。
- 「無料」表記が確認未了で全削除されている状態を補うため、cta2 に「担当者の指名・変更に対応」、cta3 に「プロフィールを見る→指名して申し込む→面談」という**裏取り済みの事実**を補足行として置き、摩擦を下げている。旧版で孤立していた「利用条件は公式サイトに記載があります。」の一文は cta3・cta4 の補足行に統合済み。

## 4. 表（table×9）の扱い

- HTMLをそのまま貼らず、**Googleドキュメントの表として作り直す**（挿入 → 表）。
- 1行目はヘッダー行として背景 #EFEFEF ＋太字。
- 列幅の目安（ページ幅を100%とした比率）：

| 表 | 列数 | 列幅比 |
|---|---|---|
| 年収の基準点 | 3 | 25 / 20 / 55 |
| SIerとSESの違い | 3 | 20 / 40 / 40 |
| SIerの4分類 | 3 | 34 / 33 / 33 |
| 転職先5タイプ比較 | 3 | 22 / 55 / 23 |
| 市場用語への変換表 | 3 | 30 / 45 / 25 |
| 職務経歴書ビフォー／アフター | 3 | 18 / 34 / 48 |
| 志望動機の3段構成 | 2 | 22 / 78 |
| 年代別の勝ち筋 | 4 | 12 / 30 / 30 / 28 |
| エージェント3類型 | 4 | 22 / 26 / 26 / 26 |

- セル内改行は使わず、長い文は句点で区切る。
- 表の直後の「※」注記は**表の外に標準テキスト**として置く（表内に入れない）。

## 5. 末尾ブロックの扱い（2026-08-30 改訂：本文HTMLから撤去済み）

旧版で本文HTML末尾に置いていた次の2ブロックは、**本文HTMLから完全に削除**した。

1. 「【社内確認用・クライアント共有版からは削除】未確定事項」→ `docs/要ファクトチェック一覧_SIer転職_20260830.md` に全量移管
2. 「【必須実装項目・未確定不可】CTAクリック計測」→ 本書「7. 計測」に全量移管

これにより「共有前に消す」という手作業に依存しなくなった。本文HTMLはそのままGoogleドキュメント化してよい。

## 6. 未確定・要確認事項の申し送り

一覧の原本は `docs/要ファクトチェック一覧_SIer転職_20260830.md`。クライアントへ渡すのは本文のみで、確認事項はチャットワーク等で別途共有する。

**公開前に確定が必要な主な項目**

- 内部リンク：「転職エージェントの担当者は変更できる？」の公開URL（本文3箇所）。`/column/` 配下はStockSun本体SEOの管理領域のため、被リンク設置は本体側へ依頼事項として起票する。
- サービス事実：求人44,000件以上・年収チャンネル登録者28.2万人の基準日（本文はいずれも「2026年8月時点／年収エージェント公式サイト」で統一済み）、指名・変更の提供条件。
- 相談条件（CVR直結・未確定のままの公開不可）：初回相談の費用範囲／オンライン面談可否／在職中相談可否／所要時間の4点。確定後、cta1〜cta4 の補足行に事実として記載する。
- 年収データ：doda各職種区分、Web系・社内SE・プライムSIerの該当区分の有無（2026-09-01のAhrefs／各種一次ソース再取得後に確認）。

## 7. 計測（公開前に必須・未確定のまま納品しない）

- 同一ドメイン内遷移のため**UTMは付与しない**（本体SEOのGA4計測を汚さないため）。
- 本文中の自社ドメインリンクおよび `/column/annual-income-agent-reputation/` へのリンクは、**ボタンCTAだけでなく本文テキストリンクも含めて全12本に計測属性を付与済み**。旧版は4本のみで、FAQ・関連記事からのクリックがGA4で分解できなかった。
- 付与している属性
  - `data-cta`：接点名（`intro_anchor` / `intro_agent` / `cta1` / `cta2` / `cta3` / `cta3_voice` / `cta4` / `faq_link` / `faq_reputation` / `related_reputation` / `related_about` / `related_voice`）
  - `data-cta-type`：`button` または `text`（ボタンCTAと本文リンクをGA4側で分離集計するため）
  - `data-article`：`sier-tenshoku`
- WP実装側で、GTMのクリックトリガー（`click` かつ要素が `a[data-cta]` にマッチ）から
  GA4カスタムイベント **`click_cta`**（パラメータ：`cta_position` = data-cta、`cta_type` = data-cta-type、`article` = data-article、`link_url` = クリックURL）を発火させる。
- GA4側でカスタムディメンション `cta_position` / `cta_type` / `article` を登録する。
- レポート時は CLAUDE.md の方針どおり**パス（/nensyuagent/ 配下）で絞り込む**。

## 8. 納品前チェック

- [ ] 文字数（現行：8,764字／タグ・空白除去、h1〜関連記事）
      ※ハウス標準の上限8,000字を764字超過。レビュー指摘（blocker3件・major多数）で必須となった加筆（比較表の難易度列・見立て方列、比較表直下CTA、生涯年収／中長期の段落、他社断定の中立化と注記、経産省の出典明記、44,000件の3点セット化、内部リンクの文脈内追加、効果保証表現の緩和と但し書き）が計約+1,370字。指摘のない箇所を約600字圧縮した上での残差であり、**これ以上の削減は情報の削除を伴う**。上限厳守を優先する場合は、削る候補（優先順）を下に記載。
      1. h2「SIerを辞めたい…」の「よく語られる不満は4つ」箇条書き（約200字）
      2. h3「SIerの4分類と…」の表＋前後文（約255字。Know KW『SIer 分類』を落とす）
      3. h3「20代：ポテンシャル採用…」の本文（約90字）
- [ ] h2×8、h3×25（2/5/4/2/2/2/6/2）、h4なし、各h2に h3 2本以上
- [ ] 表9点がGoogleドキュメントの表として崩れていない
- [ ] ボタンCTA4箇所（cta1〜cta4）がボタン相当の見た目になっている／intro はテキストリンクのまま
- [ ] 本文に編集ステータス語（要取得／一次確認中／未確定／要確認）がない（現行：0件）
- [ ] 本文に `{{}}` 記法が残っていない（現行：0件）
- [ ] 本文に社内向けブロック（メタ情報・未確定事項・計測仕様）が混入していない（現行：0件）
- [ ] 「必ず」「100%」「保証」「絶対」「無料」の各語が本文に0件（現行：0件）
- [ ] 数値はすべて〈値＋時点＋出典〉の3点セット（44,000件・28.2万人ともに「2026年8月時点／年収エージェント公式サイト」で統一済み）
